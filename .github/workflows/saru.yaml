name: java CI with maven

on:
  push:
    branches: [ main ]
    pull_request:
      branches: [ main ]     
jobs: 
  build:
  
    runs-on:ubuntu-latest
    
  steps:
    name: Checkout code
    uses: actions/checkout@v3    
      - run:
       name: Name
        echo "file is successful run"
