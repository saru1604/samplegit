name: java CI with maven

on:
  push:
    branches: [ saru ]
    pull_request:
      branches: [ saru ]     
jobs:
 
  build:
  
    runs-on:ubuntu-latest
    
     steps:
       - uses: actions/checkout@v3
    
    - run:
        echo "file is successful run"
